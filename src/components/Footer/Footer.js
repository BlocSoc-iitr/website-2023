import styles from "./Footer.module.css";

function Footer() {
    return (
        <>
            <div className={`${styles.footer}`}>
                <div className={`${styles.footerItem}`}>Connect with us</div>
            </div>
        </>

    );
}

export default Footer;